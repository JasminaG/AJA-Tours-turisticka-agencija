@{
    Layout = "~/_SiteLayout.cshtml";
    Page.Title = "EditProduct2";

    var db = Database.Open("StarterSite");
    var selectQuery = "SELECT * FROM UserProfile ORDER BY UserId";

}

<style type="text/css">
    table, th, td {
        border: solid 1px #bbbbbb;
        border-collapse: collapse;
        padding: 2px;
        background-color: white;
    }
</style>
<br /><br /><br /><br /><br />
<h6><a href="~/admin">Povratak nazad</a></h6>
<br />
<h1>Izmjeni korisnika:</h1>
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <table>
                <thead>
                    <tr>
                        <th>&nbsp;</th>
                        <th>Id</th>
                        <th>Email</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var row in db.Query(selectQuery))
                    {
                        <tr>
                            <td><a href="@Href("~/UpdateProducts2", row.UserId)">Uredi</a></td>
                            <td>@row.UserId</td>
                            <td>@row.Email</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
